// api/splitwise/auth.ts

// 1. Corrija o caminho do splitwise.ts
import { getRequestToken } from '../../lib/splitwise';

// 2. Corrija o caminho do supabase.ts
import { supabaseServer } from '../../lib/supabase';

// 3. Corrija o caminho dos types.ts (se necessário)
import { VercelRequest, VercelResponse } from '../../types';
// ...